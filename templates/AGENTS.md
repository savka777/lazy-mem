<!-- lazy-mem:start -->
## Lazy Mem

Before work, check for `.lazy-mem` in this project.
If present, read it as YAML, resolve `memory_repo` and `system_file`, then follow the Lazy Mem system instructions.
Do not preload the whole memory repo.
<!-- lazy-mem:end -->
